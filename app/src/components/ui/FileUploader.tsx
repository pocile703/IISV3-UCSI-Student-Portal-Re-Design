"use client"
// Drag-and-drop + click-to-upload file input — used in lecturer resource upload form
// Validates MIME type on client (server re-validates)
export function FileUploader() {
  return null
}
