"use client"
// useStudent — fetches the current student's profile via TanStack Query
// Placeholder: will call GET /api/student/profile

export function useStudent() {
  // TODO: replace with useQuery({ queryKey: ['student', 'profile'], queryFn: ... })
  return { data: null, isLoading: false, error: null }
}
